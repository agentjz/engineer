# P18 协调信息必须机制化

## 原则

多智能体协作时，不能靠“我说过了”“你应该知道”来协调。

协调信息必须被写成机制。

## 为什么

人类团队很多协调是靠默认默契完成的。

但智能体没有这种默契。
一个智能体改了公共接口，另一个还按旧接口继续开发，它不会自己意识到这件事。

## 在智能体工程里的含义

协调至少要外化成这些东西：

- 共享契约
- 共享状态
- 变更通知
- 阻断规则

比如：

- 哪些模块正在被改
- 哪些接口即将变化
- 哪些消费方必须同步验证

只要还靠自然语言临时提醒，说明协调机制还没长出来。
