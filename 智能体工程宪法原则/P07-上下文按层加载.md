# P07 上下文按层加载

## 原则

不是给智能体看的信息越多越好，而是越准越好。

## 为什么

很多任务跑偏，不是因为信息太少，而是因为信息太杂。

当上下文里混进太多无关内容时：

- 关键约束会被淹掉
- 智能体会在不该动的地方乱改
- 旧讨论、旧方案、旧例子会污染当前任务

## 在智能体工程里的含义

上下文至少要分三层：

- 项目级
- 模块级
- 当前任务级

执行时，只加载当前任务真正需要的那层。

项目背景可以常驻。
模块背景按需加。
任务细节只服务当前这一轮。

把所有历史对话、所有模块文档一次性塞进去，不叫上下文工程，那叫上下文堆垃圾。
